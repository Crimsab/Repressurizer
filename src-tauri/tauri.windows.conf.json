{
  "build": {
    "beforeBuildCommand": "bun run build && bun run build:sam-sidecar && bun run build:mcp-adapter"
  },
  "bundle": {
    "externalBin": ["binaries/repressurizer-sam", "binaries/repressurizer-mcp"],
    "targets": ["msi"]
  }
}
